#include <algorithm>
#include <iostream>
#include <vector>
typedef long long ll;
using namespace std;

// fast io를 위해 버퍼링을 막음.
void init() {
  cin.tie(0);
  cout.tie(0);
  ios::sync_with_stdio(false);
}

int main() {
	ll T, answer;
	string tmp;
	cin >> T;
	while(T--) {
		cin >> tmp;
		answer = 0;
		for (int i = 0; i < tmp.length() - 1; i++) {

				
		}
	}
}
