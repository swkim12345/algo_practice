/**
 * 가희와 btd5
 * 개틀링건으로 풍선을 터트림.
 * 기본적으론 일차함수꼴로 터트리지만,
 * 슈퍼모드가 있어 특정 방향으로 데미지가 10**9 이상의 공격을 하면
 * 모든 풍선을 제거할 수 있음.
 * 문제는 데미지는 어케 함? 시간 초과 날거 같은데..
 * O(NM)
 */

#include <algorithm>
#include <iostream>
#include <vector>
typedef long long ll;
using namespace std;

// fast io를 위해 버퍼링을 막음.
void init() {
  cin.tie(0);
  cout.tie(0);
  ios::sync_with_stdio(false);
}

vector<ll> 

int main() {
	ll N, M;
	cin >> N >> M;

}
